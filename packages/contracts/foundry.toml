[profile.default]
src = "src"
out = "out"
libs = ["lib", "../../node_modules"]
solc_version = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read", path = "./script" }]

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
